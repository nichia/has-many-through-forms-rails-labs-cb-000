<!-- categories/show.html.erb -->
<h1>Category: <%= @category.name %></h1>

<h2><%= pluralize(@category.posts_count, 'Post') %>:</h2>
<ul>
  <% @category.posts.each do |post| %>
    <li><%= link_to post.title, post_path(post) %> <%= post.content %></li>
  <% end %>
<ul>
